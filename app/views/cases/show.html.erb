<h2><%= @case.title %></h2>
<div id="actions"><%= link_to "Edit", edit_case_path(@case) %>|<%= link_to "Delete", @case, :confirm => "Are you sure you want to delete "+@case.title+"?\nThis action cannot be undone!", :method => :delete %>
</div>
<%= raw @case.content %>
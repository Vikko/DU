<% @comments.each do |comment| %>
	<h3><%= comment.user %></h3>
		<div id="actions"><%= link_to "Edit", edit_comment_path(comment) %>|<%= link_to "Delete", comment, :confirm => "Are you sure you want to delete this comment?\nThis action cannot be undone!", :method => :delete %></div>
	<%= comment.content %>
<% end %>
